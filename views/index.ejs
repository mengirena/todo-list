<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css" integrity="sha384-50oBUHEmvpQ+1lW4y57PTFmhCaXp0ML5d60M1M7uH2+nqUivzIebhndOJK28anvf" crossorigin="anonymous">
    
    <title>Todo List</title>
</head>
<body>
    <div class = "">
        <div>
            <h1>Welcome!</h1>
        </div>
        <div>
            <h3>You have <%= (posts.length >1) ? `${posts.length} tasks` : `${posts.length} task` %> to finish!</h3>
            <div class="tasks-table">
                <ul>
                    <% for (let i = 0; i < posts.length; i++){ %>
                        <li id= <%= posts[i].id %>><span><%= posts[i].taskName %></span><span class="delete-btn"><i class='fa fa-trash'></i></span></li>
                    <% } %>
                </ul>
            </div>
            <form action="/createTask" method="POST"> <!--action requires a / , and add method-->
                <input type="text" placeholder="task name" name="taskName">
                <input type="submit" value="Add Task">
            </form>
        </div>
    </div>
    <script src="main.js"></script>
</body>
</html>